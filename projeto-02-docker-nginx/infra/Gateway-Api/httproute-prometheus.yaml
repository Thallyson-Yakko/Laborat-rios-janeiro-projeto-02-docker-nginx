apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: react-nginx
  namespace: helm-teste
spec:
  parentRefs:
  - name: prometheus
  hostnames:
  - "app.prometheus"
  rules:
    backendRefs:
    - name: kube-prometheus-stack-prometheus
      port: 9090